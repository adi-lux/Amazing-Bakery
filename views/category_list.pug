extends layout

block content
    a(href='/') Home Page
    p Category List LOL!
    ul
        each category in categories
            li
                a(href=category.url) #{category.name}

        a(href='/categories/add')
            button Add Category